rootProject.name = 'Yi'

def componentProject = "components"

include "yi.core"
include componentProject
include "yi.editor"

file(componentProject).listFiles().each { file ->
    if (file.isDirectory()) {
        include "$componentProject:$file.name"
    }

}
