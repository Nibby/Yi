rootProject.name = 'Yi'

def componentProject = "components"

include "yi.core"
include componentProject
include "yi.editor"

file(componentProject).listFiles().each { folder ->
    include "$componentProject:$folder.name"
}
